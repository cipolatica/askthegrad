<% if alert %>
  <% if flash[:alert] == "Grads, please sign in or sign up before posting your review." %>
    <div class="alert alert-warning">
  <% else %>
    <div class="alert alert-danger">
  <% end %>
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <%= fa_icon_tag "remove" %> <%= alert %>
    <%= flash[:alert] = nil %>
  <% if flash[:alert] == "Grads, please sign in or sign up before posting your review." %>
     </div>
  <% else %>
     </div>
  <% end %>
<% end %>

<% if notice %>
  <% if flash[:notice] == "Signed in successfully." %>
    <div class="alert alert-success">
  <% else %>
    <div class="alert alert-warning">
  <% end %>
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <%= fa_icon_tag "info-sign" %> <%= notice %>
    <%= flash[:notice] = nil %>
  <% if flash[:notice] == "Signed in successfully." %>
    </div>
  <% else %>
    </div>
  <% end %>
<% end %>

<% if flash[:success] %>
    <div class="alert alert-success">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <%= fa_icon_tag "info-sign" %> <%= flash[:success] %>
      <%= flash[:success] = nil %>
    </div>
<% end %>