<% content_for :navbar, render("layouts/user_nav") %>

<%= render :layout => "layouts/minimal" do %>
  <div class="page-header">
    <div class='container'>
      <div class="row row-same-height">
        <div class="col-md-12 col-sm-height col-middle">
          <h2 class='m0'><%= @title %> <small><%= @small_text %></small></h2>
          <%= yield :description %>
        </div>
        <div class="col-md-2 col-sm-height col-middle">
          <!--THIS AREA IS WHERE THE PROGRESS TRACKER COULD POSSIBLY BE ADDED-->
          <!--<div class="progress-tracker" id="progress-tracker" style="position: relative; left: -300px">-->
            <!--< % #if @progress_image != nil && @progress_image.length %>-->
               <!--< %= #image_tag(@progress_image, height: '40', width: '220') %>-->
            <!--< % #end %>-->
          <!--</div>-->
          <%= yield :actions %>
        </div>
      </div>
    </div>
  </div><!-- /.page-header -->

  <div class='container'>
    <div class='row'>
      <%= render "layouts/flashes" %>
    </div>
  </div>

  <div class='container-fluid'>
      <div id="content" class="col-md-12">
        <%= yield %>
      </div> <!-- /#content -->
  </div>
<% end %>